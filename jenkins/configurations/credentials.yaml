credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: github-http-user
              username: ${GITHUB_USER_HTTP}
              password: ${${GITHUB_USER_HTTP_PASSWORD_ID}}
              description: "Github HTTP"
